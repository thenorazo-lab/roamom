{
  "private": true,
  "scripts": {
    "dev:backend": "cd backend && npm start",
    "dev:frontend": "cd frontend && npm start",
    "dev": "powershell -NoProfile -Command \"Start-Process -WindowStyle Minimized npm -ArgumentList 'run dev:backend'; Start-Sleep -Seconds 2; npm run dev:frontend\"",
    "android:build": "cd frontend && npm run build && npx cap sync android",
    "android:open": "cd frontend && npx cap open android",
    "android:run": "cd frontend && npm run build && npx cap sync android && npx cap open android",
    "android:devices": "adb devices",
    "android:logs": "adb logcat | Select-String \"Capacitor|chromium\""
  },
  "devDependencies": {
    "nodemon": "^3.1.11"
  }
}
